#include "Lexify.hpp"

#include <iostream>

int main() {
  /*
   * Lexify::Lexer lexer;
   * //Lexify::Lexer lexer(stream);
   *
   * lexer.setToken(0, regex, false);
   * lexer.setToken(1, regex, false);
   * lexer.setToken(2, regex, true);
   *
   * Lexity::Token* token;
   *
   * lexer.setStream(stream);
   *
   * token = lexer.getToken(sstream);
   *
   * while (token != nullptr) {
   *   token = lexer.getToken(sstream);
   *
   * }
   *
   *  struct Token {
   *    unsigned int id;
   *    attr 
   *
   *  }
   *
   */
}
